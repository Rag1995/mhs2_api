const router = require("express").Router();
// const { checkPermission } = require("../middlewares/helper");

// const Role = require("../controller/role");

// router.post("/role", checkPermission, Role.create);
// router.get("/roles", checkPermission, Role.findAll);
// router.get("/role/:id", checkPermission, Role.findById);
// router.put("/role/:id", checkPermission, Role.update);
// router.delete("/role/:id", checkPermission, Role.delete);

module.exports = router;
