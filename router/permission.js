const router = require("express").Router();
// const { checkPermission } = require("../middlewares/helper");

// const Permission = require("../controller/permission");

// router.post("/permission", checkPermission, Permission.create);
// router.get("/permissions", checkPermission, Permission.findAll);
// router.get("/permission/:id", checkPermission, Permission.findById);
// router.put("/permission/:id", checkPermission, Permission.update);
// router.delete("/permission/:id", checkPermission, Permission.delete);

module.exports = router;
