const router = require("express").Router();

// const { username, password } = require("../middlewares/validatorUser");
// const { validatorError } = require("../middlewares/validatorError");
// const User = require("../controller/user");

// router.post("/user", [username(), password()], validatorError, User.create);
// router.delete("/user/:id", User.delete);
// // router.update("/user/:id", User.update);

module.exports = router;
